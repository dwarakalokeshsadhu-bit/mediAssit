# ==============================================================================
# MedAssist Client Environment Configuration Template
# ==============================================================================

# Backend API Base URL
# Leave empty for same-origin / single-service deployments (e.g. Render, Railway, Docker).
# For decoupled deployments (e.g., Vercel frontend + Render backend), set:
# VITE_API_URL=https://your-medassist-backend.onrender.com
VITE_API_URL=
