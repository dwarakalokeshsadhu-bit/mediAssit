PORT=5000
NODE_ENV=production
MONGO_URI=mongodb+srv://<db_username>:<db_password>@cluster0.5w67tkt.mongodb.net/medassist?retryWrites=true&w=majority&appName=Cluster0
JWT_SECRET=your_jwt_secret_key
JWT_EXPIRES_IN=7d
CLIENT_URL=http://localhost:5173
GEMINI_API_KEY=

# Optional Email / SMTP Dispatch (for sending real 6-digit password reset verification codes)
# If left blank, the system logs verification codes to the console and provides test preview
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM="MedAssist Healthcare" <noreply@medassist.clinic>
